import { BackgroundInterface, BorderInterface } from "../interfaces/interface";
export const initialBackground: BackgroundInterface = {
  backgroundColor: "var(--cyan)",
};
export const initialBorder: BorderInterface = {
  border: "none",
  color: "var(--very-dark-blue)",
};
